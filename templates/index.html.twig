<!DOCTYPE html>
<html>
    <head>
        <title>Home</title>
    </head>
    <body>
        <h1>{{ page_title }}</h1>
        {% if is_logged_in %}
            Hello {{ nama }} - {{email}}!
            <br>
            Anda login sebagai: {{ role }}
            , <a href="/logout">logout</a>
        {% else %}
            Hello Guest!, silakan <a href="/login">login</a>
        {% endif %}
    </body>
</html>